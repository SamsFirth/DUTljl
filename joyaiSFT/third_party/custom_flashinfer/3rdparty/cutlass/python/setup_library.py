from setuptools import setup

def perform_setup():
    setup(name='cutlass_library', version='3.6.0', description='CUTLASS library generation scripts', packages=['cutlass_library'])
if __name__ == '__main__':
    perform_setup()